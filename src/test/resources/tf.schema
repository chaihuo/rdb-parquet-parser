message tf {
  required BINARY CONTRACTID;
  required BINARY BUSINESSTIME;
  optional DOUBLE OPENPRICE;
  optional DOUBLE LASTPRICE;
  optional DOUBLE HIGHPRICE;
  optional DOUBLE LOWPRICE;
  optional DOUBLE SETTLEPRICE;
  optional DOUBLE PRESETTLE;
  optional DOUBLE CLOSEPRICE;
  optional DOUBLE PRECLOSE;
  optional DOUBLE CQ;
  optional DOUBLE VOLUME;
  optional DOUBLE CM;
  optional DOUBLE AMOUNT;
  optional DOUBLE PREPOSITION;
  optional DOUBLE POSITION;
  optional DOUBLE POSITIONCHANGE;
  optional DOUBLE LIMITUP;
  optional DOUBLE LIMITDOWN;
  optional INT64 SIDE;
  optional INT64 OC;
  optional DOUBLE B01;
  optional DOUBLE B02;
  optional DOUBLE B03;
  optional DOUBLE B04;
  optional DOUBLE B05;
  optional DOUBLE S01;
  optional DOUBLE S02;
  optional DOUBLE S03;
  optional DOUBLE S04;
  optional DOUBLE S05;
  optional DOUBLE BV01;
  optional DOUBLE BV02;
  optional DOUBLE BV03;
  optional DOUBLE BV04;
  optional DOUBLE BV05;
  optional DOUBLE SV01;
  optional DOUBLE SV02;
  optional DOUBLE SV03;
  optional DOUBLE SV04;
  optional DOUBLE SV05;
  optional DOUBLE CURRDELTA;
  optional DOUBLE PREDELTA;
  optional INT64 SETTLEMENTGROUPID;
  optional INT64 SETTLEMENTID;
  optional DOUBLE CHANGE;
  optional DOUBLE CHANGERATIO;
  optional INT64 CONTINUESIGN;
  optional BINARY TRADINGDATE;
  optional BINARY LOCALTIME;
  optional BINARY RECTIME;
  optional BINARY EXCHANGECODE;
  optional INT64 ID;
  optional BINARY UNIX;
}
