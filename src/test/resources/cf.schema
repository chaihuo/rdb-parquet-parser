message cf {
  required BINARY CONTRACTID;
  required BINARY TDATETIME;
  optional BINARY CONTRACTNAME;
  optional DOUBLE LASTPX;
  optional DOUBLE HIGHPX;
  optional DOUBLE LOWPX;
  optional DOUBLE CQ;
  optional DOUBLE TQ;
  optional DOUBLE LASTQTY;
  optional DOUBLE INITOPENINTS;
  optional DOUBLE OPENINTS;
  optional DOUBLE INTSCHG;
  optional DOUBLE TURNOVER;
  optional DOUBLE RISELIMIT;
  optional DOUBLE FALLLIMIT;
  optional DOUBLE PRESETTLE;
  optional DOUBLE PRECLOSE;
  optional DOUBLE OPENPX;
  optional DOUBLE CLOSEPX;
  optional DOUBLE SETTLEMENTPX;
  optional DOUBLE LIFELOW;
  optional DOUBLE LIFEHIGH;
  optional DOUBLE AVGPX;
  optional DOUBLE BIDIMPLYQTY;
  optional DOUBLE ASKIMPLYQTY;
  optional BINARY SIDE;
  optional DOUBLE S1;
  optional DOUBLE B1;
  optional DOUBLE SV1;
  optional DOUBLE BV1;
  optional DOUBLE S5;
  optional DOUBLE S4;
  optional DOUBLE S3;
  optional DOUBLE S2;
  optional DOUBLE B2;
  optional DOUBLE B3;
  optional DOUBLE B4;
  optional DOUBLE B5;
  optional DOUBLE SV5;
  optional DOUBLE SV4;
  optional DOUBLE SV3;
  optional DOUBLE SV2;
  optional DOUBLE BV2;
  optional DOUBLE BV3;
  optional DOUBLE BV4;
  optional DOUBLE BV5;
  optional DOUBLE PREDELTA;
  optional DOUBLE CURRDELTA;
  optional DOUBLE CHG;
  optional DOUBLE CHGPCT;
  optional BINARY VARIETIES;
  optional BINARY LOCALTIME;
  optional BINARY TRADINGDATE;
  optional BINARY MFLXID;
  optional BINARY MARKET;
  optional BINARY UNIX;
}
